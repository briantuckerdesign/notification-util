import { configure, config } from './config';
import { Spinner } from './spinner';
import { Notification } from './Notification';

export default {
  Spinner,
  Notification,
  configure,
  config
};
